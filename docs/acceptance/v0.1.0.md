# Codex Quota v0.1.0 验收记录

> 状态说明：`PASS` 只用于已有可复核证据；`PENDING` 表示等待人工执行；`NOT RUN` 表示本次未运行。本文档不以自动检查替代人工视觉或安装验收。

## 测试环境

| 项目 | 记录 |
| --- | --- |
| 验收日期 | 2026-07-22 |
| 测试人员 | yi / Codex |
| Mac 型号 | 未记录（不影响本轮架构与窗口证据） |
| 架构（Apple Silicon / Intel） | Apple Silicon（arm64）；发布物同时验证 x86_64 |
| macOS 版本 | 26.5.1 |
| 官方 Codex 版本 | 26.715.71837（bundle id `com.openai.codex`，本机显示名 ChatGPT） |
| Codex Quota 构建 | v0.1.0 |
| DMG SHA-256 | `30ca37544c8fd1b5770788fb9b9e7eb9889cae5c4887b517314bd22ae8df096d`（从 `5e3287d` 最终源码构建，已重新核对） |

## 自动与发布物证据

| 检查 | 状态 | 方法与证据 |
| --- | --- | --- |
| 仓库策略 | PASS | 2026-07-22 运行 `zsh Tests/Scripts/repository-policy-test.sh`，输出 `PASS: public repository policy` |
| 构建脚本策略 | PASS | 2026-07-22 运行 `zsh Tests/Scripts/build-scripts-test.sh`，输出 `PASS: release build script policy` |
| Workflow YAML 基本解析 | PASS | 2026-07-22 使用 Ruby Psych 解析 `ci.yml` 与 `release.yml`，无语法错误 |
| Release 标签版本校验 | PASS | `v0.1.0` 解析为 `0.1.0`；含 shell 元字符的伪标签被校验拒绝 |
| Info.plist | PASS | 2026-07-22 运行 `plutil -lint Resources/Info.plist`，输出 `OK` |
| 扫描失败传播 | PASS（独立验证） | Swift 6 严格并发与 warnings-as-errors 下验证：全根缺失和无有效快照的注入读取失败会抛错；合法空目录返回空；坏文件不遮蔽另一根的有效周快照 |
| 完整 XCTest | NOT RUN | 本机 Command Line Tools 编译器与 SDK build 不匹配，测试在发现 XCTest 前即被工具链阻断；等待标准 Xcode CI |
| GitHub Actions CI | NOT RUN | 尚未 push，不存在远端 CI 结果 |
| Universal DMG 构建与校验 | PASS | 2026-07-22 从 `5e3287d` 的干净归档重新构建；两次 release 编译完成，DMG 内 app 由 `verify-release.sh` 验证为 `x86_64 arm64` |
| 本轮重新构建 DMG | PASS | `build-dmg.sh 0.1.0` exit 0；`shasum -a 256 -c` 输出 `OK`；最终 DMG 为 2,219,252 bytes |

## 首次安装与权限

| 场景 | 状态 | 实际结果 / 证据 |
| --- | --- | --- |
| 从 DMG 拖入“应用程序” | NOT RUN | 本轮挂载最终 DMG 后用 `ditto --noextattr --noqtn` 安装到 `/Applications`；拖拽手势未执行 |
| 首次右键选择“打开” | NOT RUN | 本轮通过终端启动已安装应用，未执行 Finder 右键动作 |
| 未公证提示与 README 步骤一致 | NOT RUN | 本机此前已运行开发构建，无法复现干净机器首次 Gatekeeper 流程 |
| 首次说明准确描述五字段、无网络和权限用途 | NOT RUN | onboarding 已记录为完成；公开文案由仓库策略测试覆盖，未替代首次视觉验收 |
| 授予辅助功能权限后恢复定位 | PASS | 删除旧签名身份后，从 `/Applications/Codex Quota.app` 重新添加；重启后浮层窗口真实出现 |
| 直接启动官方 Codex，无定制启动器 | PASS | 官方 `com.openai.codex` 由系统正常启动/置前；未使用 Codex++ 或定制启动器 |

## 状态与视觉

| 场景 | 状态 | 实际结果 / 证据 |
| --- | --- | --- |
| 正常（绿色，剩余 > 30%） | PASS（自动 + 运行） | 本地周窗口快照 `usedPercent=14`，对应剩余 86%；前台时实际生成 30 pt 高胶囊窗口。颜色分支由展示测试覆盖，未采集含聊天内容的截图 |
| 提醒（琥珀色，剩余 10%–30%） | PENDING | NOT RUN |
| 紧急（红色，剩余 < 10%） | PENDING | NOT RUN |
| 等待数据 | PASS（自动） | 展示状态与布局测试覆盖；未人工替换真实缓存 |
| 数据可能已过期 | PASS（自动） | 展示状态与布局测试覆盖；未人工替换真实缓存 |
| 重置时间已过，等待刷新 | PASS（自动） | 展示状态与布局测试覆盖；未人工替换真实缓存 |
| 浅色外观 | PENDING | NOT RUN |
| 深色外观 | PENDING | NOT RUN |

## 窗口、屏幕与生命周期

| 场景 | 状态 | 实际结果 / 证据 |
| --- | --- | --- |
| 侧边栏展开，胶囊不覆盖主内容 | PASS | 官方窗口 `1550,213,1501×820` 时胶囊为 `1656,991,92×30`；位于左下侧栏范围内。较早一次同样得到 `1579,205,1501×820` / `1633,981,165×30` |
| 侧边栏收起 | PENDING | NOT RUN；详细 AX 树下应安全隐藏。当前 Codex 若仅暴露极简 AX 树，应用无法可靠区分展开与收起，需从菜单栏关闭悬浮层 |
| 移动窗口，停止后准确贴合 | PENDING | NOT RUN |
| 缩放窗口 | PENDING | NOT RUN |
| 最小化与恢复 | PENDING | NOT RUN |
| 全屏与退出全屏 | PENDING | NOT RUN |
| 多显示器与不同缩放比例 | PENDING | NOT RUN |
| 切换空间与前台应用 | PARTIAL PASS | 切到 Finder 后无 Codex Quota layer-3 胶囊；切回官方 Codex 后 layer-3 胶囊恢复。空间切换未运行 |
| 官方 Codex 退出并重启 | PENDING | NOT RUN |
| Codex Quota 退出并重启 | PASS | 重新授权后退出并正常启动 `/Applications/Codex Quota.app`，新进程生成浮层 |
| 登录项开启、重启登录并自动启动 | PENDING | NOT RUN |
| 登录项关闭后不再自动启动 | PENDING | NOT RUN |

## 卸载

| 场景 | 状态 | 实际结果 / 证据 |
| --- | --- | --- |
| 关闭并移除登录项 | PENDING | NOT RUN |
| 删除应用 | PENDING | NOT RUN |
| 删除 Application Support 缓存 | PENDING | NOT RUN |
| 删除 Logs | PENDING | NOT RUN |
| Codex 原始数据未受影响 | PENDING | NOT RUN |

## 结论

**PARTIAL PASS** — 最终 DMG、双架构、校验和、直接启动官方 Codex、重新授权、真实浮层出现、前台隐藏/恢复和应用重启已有可复核证据。窗口实机证据采自 `a7d2d56`；之后只修改核心扫描错误传播，窗口与浮层源码未变化，最终发布物从 `5e3287d` 构建。Finder 拖拽/右键首次打开、提醒与紧急颜色人工视觉、窗口移动缩放/全屏/多显示器、登录项和卸载仍未运行；首版发布不把这些项目虚报为通过。

已知隐私优先限制：极简 AX 树没有足够的侧边栏状态证据。应用不会用截图、OCR、对话值或其他内容探测补足该信息，而是保留菜单栏悬浮层开关与位置偏移作为人工控制。
